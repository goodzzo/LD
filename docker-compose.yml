services:
  db:
    image: postgres:16
    container_name: ld-postgres
    environment:
      POSTGRES_USER: ld
      POSTGRES_PASSWORD: ld
      POSTGRES_DB: ld
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
